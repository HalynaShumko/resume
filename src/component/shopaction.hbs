<div class="col-12 m-3">
    {{#with service}}
    <div class="card text-bg-success text-center p-3">
        <h2>{{title}}</h2>
        <p class="pt-3 text-center">{{description}}</p>
        <div class="text-center">
            {{#each buttons}}
            <a href="{{link}}{{rel}}" class="btn btn-primary">{{text}}</a>
            {{/each}}
        </div>
    </div>
    {{/with}}
</div>